{
  "name": "traveloptimizer",
  "version": "1.2.0",
  "description": "Flight search aggregator using Node.js and React",
  "main": "server/index.js",
  "engines": {
    "node": ">=14.0.0"
  },
  "scripts": {
    "start": "node server/index.js",
    "test": "echo \"No tests specified\"",
    "build": "mkdir -p dist && cp -r public server package.json dist/",
    "deploy": "npm run build && node server/index.js"
  },
  "dependencies": {
    "axios": "^1.9.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "serve-static": "^1.15.0"
  }
}
